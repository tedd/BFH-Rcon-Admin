<UserControl x:Class="Kernel.BFHAdmin.Client.GUI.Views.DebugDataModel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="clr-namespace:Xceed.Wpf.Toolkit.PropertyGrid;assembly=Xceed.Wpf.Toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!--<xctk:PropertyGrid Grid.Column="0" SelectedObject="{Binding RconClient, Mode=OneWay}" IsReadOnly="true"  />
        <xctk:PropertyGrid Grid.Column="1" SelectedObject="{Binding RconClient.ServerInfoCommand.ServerInfo, Mode=OneWay}" IsReadOnly="true"  />-->
        
    </Grid>
</UserControl>
